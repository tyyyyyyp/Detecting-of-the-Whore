if (keyboard_check_pressed(ord("E"))) {
if (mana >= 20) {
    // Перевіряємо, чи здоров'я менше за 100
    if (health < 100) {
        // Віднімаємо 20 мани
        mana -= 20;

        // Додаємо 30 хп, але не перевищуємо 100
        health = min(health + 30, 100);

        // Можна додати звуковий ефект або анімацію для візуалізації взаємодії
        audio_play_sound(snd_heal, 0, false);
    } else {
        // Якщо здоров'я вже на максимумі
      
    }
} else {
    // Якщо мани не вистачає
   
}
}