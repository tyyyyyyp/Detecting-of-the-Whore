// Затуманення екрану
if (fading_in)
{
    alpha += fade_speed;
    if (alpha >= 1) 
    {
        alpha = 1;  // Досягаємо максимальної прозорості (чорний екран)
        // Починаємо відлік часу після того, як екран затуманився
        fade_time += 1;
    }
}
// Висвітлення екрану
else
{
    alpha -= fade_speed;
    if (alpha <= 0) alpha = 0; // Мінімальна прозорість (повернення до прозорого екрану)
}

// Якщо екран затуманився і пройшло достатньо часу, переходимо до наступної кімнати
if (fade_time > room_sped * 2)  // Після 2 секунд
{
    room_goto_next(); // Перехід до наступної кімнати
}
