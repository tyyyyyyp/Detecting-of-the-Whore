// Перевіряємо, чи з моменту натискання пройшло менше 10 секунд
if (text_time != -1 && text_time < 10 * room_speed) {
    // Малюємо текст
    draw_text(x + 100, y - 100, "Ви натиснули 'E'!");
    
    // Оновлюємо таймер
    text_time += 1;
}
