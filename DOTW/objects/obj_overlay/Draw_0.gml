// Перевірка, чи існує глобальна змінна fade_alpha
if (!variable_global_exists("fade_alpha")) {
    global.fade_alpha = 0; // Ініціалізація глобальної змінної, якщо вона ще не визначена
}

// Застосовуємо глобальну прозорість
draw_set_alpha(global.fade_alpha); // Застосовуємо глобальну прозорість

// Малюємо всі об'єкти, включаючи себе
draw_self();  // Цей код дозволяє об'єкту малювати себе на екрані
